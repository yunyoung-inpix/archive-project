// import { DataProps } from "./SearchInput";

// const SearchResult = ({ data }: DataProps): React.ReactElement => {
//   const { blogname, title, contents, thumbnail, url } = data;

//   return <div>fff</div>;
// };

const SearchResult = () => {
  return <div>fff</div>;
};

export default SearchResult;
